---
// FAQ component using text configuration
import { getTexts } from '../utils/textConfig';

const texts = getTexts();
const faq = texts.faq;
const company = texts.company;
const ariaLabels = texts.ariaLabels;
---

<section class="faq">
  <div class="faq-container">
    <div class="section-header">
      <div class="header-badge">
        <span>Häufige Fragen</span>
      </div>
      <h2>Alles was Sie über unseren Schlüsseldienst wissen müssen</h2>
      <p class="section-description">
        {faq.description}
      </p>
    </div>
    
    <div class="faq-grid">
      {faq.items.map((faqItem, index) => (
        <div class="faq-item" aria-label={`${ariaLabels.faq.faqItem} ${index + 1}`}>
          <div class="faq-header">
            <div class="question-number" aria-hidden="true">
              {(index + 1).toString().padStart(2, '0')}
            </div>
            <h3 class="faq-question">{faqItem.question}</h3>
          </div>
          <div class="faq-answer">
            <p>{faqItem.answer}</p>
          </div>
        </div>
      ))}
    </div>
    
    <div class="faq-contact">
      <div class="contact-content">
        <h3>Haben Sie weitere Fragen?</h3>
        <p>
          Unser erfahrenes Team steht Ihnen jederzeit zur Verfügung. Rufen Sie uns an für eine kostenlose Beratung oder einen Kostenvoranschlag.
        </p>
        <div class="contact-actions">
          <button class="contact-btn primary" onclick="window.location.href='tel:+491604996655'" aria-label={ariaLabels.faq.ctaButton}>
            <span class="btn-text">Jetzt anrufen: +49 1604 996655</span>
            <span class="btn-subtext">Kostenlose Beratung</span>
          </button>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .faq {
    padding: var(--spacing-20) var(--spacing-6);
    background: var(--gray-50);
  }
  
  .faq-container {
    max-width: 1200px;
    margin: 0 auto;
  }
  
  .section-header {
    text-align: center;
    max-width: 800px;
    margin: 0 auto var(--spacing-16);
  }
  
  .header-badge {
    display: inline-flex;
    align-items: center;
    background: linear-gradient(135deg, var(--secondary-blue), var(--accent-blue));
    color: white;
    padding: var(--spacing-2) var(--spacing-4);
    border-radius: var(--radius-xl);
    font-size: var(--font-sizes-sm);
    font-weight: 600;
    margin-bottom: var(--spacing-6);
  }
  
  .faq h2 {
    font-size: var(--font-sizes-4xl);
    margin-bottom: var(--spacing-6);
    color: var(--gray-900);
    line-height: 1.2;
  }
  
  .section-description {
    font-size: var(--font-sizes-lg);
    color: var(--gray-600);
    line-height: 1.6;
    margin: 0;
  }
  
  .faq-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: var(--spacing-6);
    margin-bottom: var(--spacing-16);
  }
  
  .faq-item {
    background: white;
    padding: var(--spacing-6);
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-md);
    transition: all 0.3s ease;
    border: 1px solid var(--gray-200);
    position: relative;
  }
  
  .faq-item:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
    border-color: var(--primary-blue-light);
  }
  
  .faq-header {
    display: flex;
    align-items: flex-start;
    gap: var(--spacing-4);
    margin-bottom: var(--spacing-4);
  }
  
  .question-number {
    background: linear-gradient(135deg, var(--primary-blue), var(--primary-blue-light));
    color: white;
    width: 36px;
    height: 36px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
    font-size: var(--font-sizes-sm);
    flex-shrink: 0;
  }
  
  .faq-question {
    font-size: var(--font-sizes-lg);
    font-weight: 700;
    color: var(--gray-900);
    margin: 0;
    line-height: 1.3;
  }
  
  .faq-answer {
    color: var(--gray-600);
    line-height: 1.6;
    padding-left: calc(36px + var(--spacing-4));
  }
  
  .faq-answer p {
    margin: 0;
  }
  
  .faq-contact {
    background: linear-gradient(135deg, white, var(--gray-100));
    padding: var(--spacing-10);
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-lg);
    border: 1px solid var(--gray-200);
  }
  
  .contact-content h3 {
    font-size: var(--font-sizes-3xl);
    font-weight: 700;
    margin-bottom: var(--spacing-4);
    color: var(--gray-900);
  }
  
  .contact-content p {
    font-size: var(--font-sizes-lg);
    color: var(--gray-600);
    margin-bottom: var(--spacing-6);
    line-height: 1.6;
  }
  
  .contact-actions {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-3);
  }
  
  .contact-btn {
    padding: var(--spacing-4) var(--spacing-6);
    border-radius: var(--radius-lg);
    font-weight: 600;
    transition: all 0.2s ease;
    text-align: center;
  }
  
  .contact-btn.primary {
    background: linear-gradient(135deg, var(--success), var(--success-light));
    color: white;
    box-shadow: var(--shadow-md);
  }
  
  .contact-btn.primary:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-lg);
  }
  
  .btn-text {
    display: block;
    font-size: var(--font-sizes-lg);
    margin-bottom: var(--spacing-1);
  }
  
  .btn-subtext {
    display: block;
    font-size: var(--font-sizes-sm);
    opacity: 0.9;
  }
  
  .contact-btn.secondary {
    background: var(--gray-100);
    color: var(--gray-700);
    border: 1px solid var(--gray-300);
  }
  
  .contact-btn.secondary:hover {
    background: var(--gray-200);
    border-color: var(--primary-blue);
    color: var(--primary-blue);
  }
  
  .contact-image {
    position: relative;
    border-radius: var(--radius-lg);
    overflow: hidden;
    box-shadow: var(--shadow-md);
  }
  
  .contact-image img {
    width: 100%;
    height: 250px;
    object-fit: cover;
  }
  
  @media (max-width: 768px) {
    .faq {
      padding: var(--spacing-16) var(--spacing-4);
    }
    
    .faq-grid {
      grid-template-columns: 1fr;
      gap: var(--spacing-4);
    }
    
    .faq h2 {
      font-size: var(--font-sizes-3xl);
    }
    
    .section-description {
      font-size: var(--font-sizes-base);
    }
    
    .faq-contact {
      grid-template-columns: 1fr;
      gap: var(--spacing-6);
      padding: var(--spacing-6);
    }
    
    .contact-content h3 {
      font-size: var(--font-sizes-2xl);
    }
    
    .contact-actions {
      flex-direction: column;
    }
  }
</style> 